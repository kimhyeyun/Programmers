#include<iostream>
#include <string>
#include <vector>

using namespace std;

int answer = 0;

void DFS(vector<int> & numbers, int target,int sum, int idx) {
    if(idx==)
}

int solution(vector<int> numbers, int target) {
    

    return answer;
}